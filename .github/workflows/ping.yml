name: KeepAlive
on:
  schedule:
    - cron: '*/14 * * * *' # Стучит в дверь каждые 14 минут
  workflow_dispatch:      # Добавляет кнопку ручного запуска

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send request
        run: curl -I https://qwallity-chatbot.onrender.com
